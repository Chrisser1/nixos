{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; echo \"$f\" | grep -qE '\\.nix$' && nix run nixpkgs#alejandra -- \"$f\"; } 2>/dev/null || true",
            "timeout": 60,
            "statusMessage": "Formatting .nix file..."
          }
        ]
      }
    ]
  }
}
